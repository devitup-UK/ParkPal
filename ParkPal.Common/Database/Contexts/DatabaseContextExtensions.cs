using System.Linq;
using Microsoft.EntityFrameworkCore;

namespace ParkPal.Common.Database.Contexts;

public static class DatabaseContextExtensions
{
    // public static ForgottenPasswordRequest? GetByToken(this DbSet<ForgottenPasswordRequest> forgottenPasswordRequests, string token)
    // {
    //     return forgottenPasswordRequests.FirstOrDefault(a => a.Token == token);
    // }
}